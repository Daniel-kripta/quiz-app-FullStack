# Examen Tipo Test — Módulo 1: React (Frontend Development)

---

## SECCIÓN 1: PREGUNTAS BÁSICAS (1–70)

**1.** ¿Qué significa HTML?
- A) Hyper Transfer Markup Language
- B) HyperText Markup Language
- C) Hyper Tool Markup Language
- D) HyperText Making Language

**2.** ¿Cuál es la etiqueta HTML correcta para el título más importante de una página?
- A) `<title>`
- B) `<h6>`
- C) `<h1>`
- D) `<header>`

**3.** ¿Qué etiqueta HTML se usa para crear un enlace?
- A) `<link>`
- B) `<a>`
- C) `<href>`
- D) `<nav>`

**4.** ¿Cuál es la propiedad CSS para cambiar el color del texto?
- A) `font-color`
- B) `text-color`
- C) `color`
- D) `foreground`

**5.** ¿Qué valor de `display` convierte un elemento en contenedor flexbox?
- A) `display: block`
- B) `display: inline`
- C) `display: flex`
- D) `display: grid`

**6.** En CSS, ¿qué propiedad controla el espacio entre el contenido y el borde de un elemento?
- A) `margin`
- B) `padding`
- C) `border`
- D) `spacing`

**7.** ¿Cuál es la diferencia entre `margin` y `padding`?
- A) `margin` es espacio interior, `padding` es espacio exterior
- B) `padding` es espacio interior, `margin` es espacio exterior
- C) Son lo mismo
- D) `margin` aplica solo a textos

**8.** ¿Qué comando de Git inicializa un repositorio nuevo?
- A) `git start`
- B) `git create`
- C) `git init`
- D) `git new`

**9.** ¿Cuál es el comando para añadir todos los archivos al área de staging en Git?
- A) `git commit -a`
- B) `git add .`
- C) `git push all`
- D) `git stage all`

**10.** ¿Qué comando de Git guarda los cambios en el repositorio local?
- A) `git save`
- B) `git push`
- C) `git commit -m "mensaje"`
- D) `git store`

**11.** En JavaScript, ¿cuál es la diferencia entre `let` y `const`?
- A) `let` es para números, `const` es para strings
- B) `const` no puede ser reasignado, `let` sí puede
- C) `let` es más rápido que `const`
- D) No hay diferencia

**12.** ¿Qué devuelve `typeof null` en JavaScript?
- A) `"null"`
- B) `"undefined"`
- C) `"object"`
- D) `"boolean"`

**13.** ¿Cómo se declara una función flecha en JavaScript?
- A) `function => () {}`
- B) `const fn = () => {}`
- C) `arrow fn() {}`
- D) `fn => function() {}`

**14.** ¿Qué método de array devuelve un nuevo array con los elementos que cumplen una condición?
- A) `map()`
- B) `find()`
- C) `filter()`
- D) `reduce()`

**15.** ¿Qué hace el método `map()` en un array?
- A) Filtra elementos según una condición
- B) Transforma cada elemento y devuelve un nuevo array del mismo tamaño
- C) Busca un elemento por índice
- D) Ordena el array

**16.** ¿Cómo se accede al primer elemento de un array llamado `items`?
- A) `items.first()`
- B) `items[1]`
- C) `items[0]`
- D) `items.get(0)`

**17.** ¿Qué es el DOM?
- A) Un lenguaje de programación
- B) La representación en árbol del documento HTML que el navegador crea en memoria
- C) Un framework de JavaScript
- D) Un método de CSS

**18.** ¿Cómo se selecciona un elemento con id `"titulo"` en JavaScript?
- A) `document.getElement("titulo")`
- B) `document.querySelector(".titulo")`
- C) `document.getElementById("titulo")`
- D) `document.select("#titulo")`

**19.** ¿Qué hace `addEventListener`?
- A) Crea un nuevo elemento HTML
- B) Registra una función que se ejecuta cuando ocurre un evento
- C) Elimina un elemento del DOM
- D) Modifica el CSS de un elemento

**20.** ¿Qué evento se dispara cuando el usuario hace clic en un elemento?
- A) `hover`
- B) `focus`
- C) `click`
- D) `press`

**21.** ¿Qué es JSX?
- A) Un lenguaje de estilos para React
- B) Una extensión de sintaxis de JavaScript que permite escribir HTML dentro de JS
- C) Un gestor de paquetes de React
- D) Un tipo de componente en React

**22.** ¿Cómo se crea un componente funcional básico en React?
- A) `class MyComponent {}`
- B) `const MyComponent = () => <div>Hola</div>`
- C) `component MyComponent() {}`
- D) `React.create('MyComponent', () => {})`

**23.** ¿Cómo se pasa información de un componente padre a uno hijo en React?
- A) Mediante state global
- B) Mediante props
- C) Mediante eventos del DOM
- D) Mediante variables globales

**24.** ¿Cómo se usa una variable JavaScript dentro de JSX?
- A) Con comillas dobles: `"variable"`
- B) Con llaves: `{variable}`
- C) Con paréntesis: `(variable)`
- D) Con corchetes: `[variable]`

**25.** ¿Cuál es el comando para crear un proyecto React con Vite?
- A) `npx create-react-app mi-app`
- B) `npm create vite@latest mi-app`
- C) `npm init react mi-app`
- D) `npx vite new mi-app`

**26.** ¿Qué extensión de archivo usa React por convención para componentes con JSX?
- A) `.html`
- B) `.jsx` o `.tsx`
- C) `.rsx`
- D) `.reactjs`

**27.** En CSS Flexbox, ¿qué propiedad alinea elementos en el eje principal?
- A) `align-items`
- B) `justify-content`
- C) `flex-direction`
- D) `align-content`

**28.** ¿Qué propiedad CSS hace que los elementos flex se envuelvan en múltiples líneas?
- A) `flex-wrap: wrap`
- B) `flex-flow: multi`
- C) `flex-break: true`
- D) `flex-lines: wrap`

**29.** ¿Para qué sirve `grid-template-columns` en CSS Grid?
- A) Para definir el número de filas
- B) Para definir la estructura de columnas del grid
- C) Para alinear el contenido horizontalmente
- D) Para añadir espaciado entre celdas

**30.** ¿Qué unidad CSS es relativa al tamaño de fuente del elemento padre?
- A) `px`
- B) `em`
- C) `vh`
- D) `%`

**31.** ¿Qué hace la media query `@media (max-width: 768px)`?
- A) Aplica estilos cuando el viewport tiene más de 768px
- B) Aplica estilos cuando el viewport tiene 768px o menos
- C) Establece el ancho máximo del contenedor
- D) Aplica estilos solo en tablets

**32.** ¿Qué es `npm`?
- A) Node Package Manager, gestor de paquetes de Node.js
- B) New Project Manager
- C) Node Process Manager
- D) Network Protocol Manager

**33.** ¿Qué archivo de Git se usa para ignorar archivos del repositorio?
- A) `.gitconfig`
- B) `.gitkeep`
- C) `.gitignore`
- D) `.gitexclude`

**34.** ¿Qué comando crea una nueva rama en Git?
- A) `git branch nueva-rama`
- B) `git create nueva-rama`
- C) `git fork nueva-rama`
- D) `git split nueva-rama`

**35.** ¿Qué hace `git merge`?
- A) Elimina una rama
- B) Combina los cambios de una rama en otra
- C) Crea un commit vacío
- D) Revierte el último commit

**36.** En JavaScript, ¿cómo se convierte un string a número?
- A) `string.toNumber()`
- B) `parseInt(string)` o `Number(string)`
- C) `string.parse()`
- D) `convert(string, 'number')`

**37.** ¿Qué hace el operador de spread `...` en JavaScript?
- A) Declara una variable de solo lectura
- B) Expande los elementos de un array u objeto
- C) Crea una copia profunda de un objeto
- D) Elimina propiedades de un objeto

**38.** ¿Qué es la desestructuración en JavaScript?
- A) Eliminar propiedades de un objeto
- B) Extraer valores de arrays u objetos en variables individuales
- C) Convertir un objeto en array
- D) Ordenar las propiedades de un objeto

**39.** ¿Qué devuelve `[1, 2, 3].find(x => x > 1)`?
- A) `[2, 3]`
- B) `true`
- C) `2`
- D) `1`

**40.** ¿Qué es una Promise en JavaScript?
- A) Una variable que no puede cambiar
- B) Un objeto que representa el resultado eventual de una operación asíncrona
- C) Una función que se ejecuta de forma síncrona
- D) Un tipo de bucle asíncrono

**41.** ¿Qué hace `async/await`?
- A) Convierte código asíncrono en síncrono bloqueante
- B) Permite escribir código asíncrono con apariencia síncrona
- C) Ejecuta funciones en paralelo siempre
- D) Cancela operaciones asíncronas

**42.** ¿Qué método se usa para hacer una petición HTTP en JavaScript moderno?
- A) `http.get()`
- B) `XMLHttpRequest()`
- C) `fetch()`
- D) `request()`

**43.** ¿Qué devuelve `fetch(url)`?
- A) Los datos directamente
- B) Una Promise
- C) Un objeto Response síncrono
- D) Un string JSON

**44.** ¿Cómo se convierte la respuesta de `fetch` a JSON?
- A) `response.toJSON()`
- B) `JSON.parse(response)`
- C) `response.json()` (devuelve una Promise)
- D) `response.data()`

**45.** En React, ¿qué es el estado (state)?
- A) Las props que recibe un componente
- B) Datos internos del componente que pueden cambiar y causan re-renderizado
- C) Los estilos CSS del componente
- D) El HTML que genera el componente

**46.** ¿Qué hook de React se usa para manejar estado local?
- A) `useEffect`
- B) `useContext`
- C) `useState`
- D) `useRef`

**47.** ¿Qué devuelve `useState(0)`?
- A) El valor inicial `0`
- B) Un array con el valor actual y una función para actualizarlo
- C) Un objeto con `value` y `setValue`
- D) Solo la función actualizadora

**48.** ¿Cómo se renderiza una lista en React?
- A) Con un bucle `for` directamente en JSX
- B) Con el método `map()` que devuelve elementos JSX
- C) Con la directiva `v-for`
- D) Con `React.forEach()`

**49.** ¿Por qué es necesaria la prop `key` al renderizar listas en React?
- A) Para aplicar estilos a cada elemento
- B) Para que React identifique de forma única cada elemento y optimice el re-renderizado
- C) Es obligatoria por sintaxis JSX
- D) Para ordenar los elementos

**50.** ¿Qué es un componente controlado en React?
- A) Un componente que no tiene estado
- B) Un formulario cuyo valor está controlado por el estado de React
- C) Un componente que solo recibe props
- D) Un componente con acceso al DOM nativo

**51.** ¿Para qué sirve la etiqueta `<form>` en HTML?
- A) Para mostrar datos en una tabla
- B) Para crear un contenedor de entradas de usuario que puede enviarse
- C) Para crear navegación entre páginas
- D) Para incrustar contenido externo

**52.** ¿Qué atributo HTML es necesario para asociar un `<label>` con un `<input>`?
- A) El atributo `name` del input
- B) El atributo `for` del label y el `id` del input
- C) El atributo `class` compartido
- D) El atributo `type` del input

**53.** ¿Cuál es la diferencia entre `==` y `===` en JavaScript?
- A) `==` compara valor y tipo, `===` solo valor
- B) `===` compara valor y tipo, `==` solo valor (con coerción)
- C) Son equivalentes en todos los casos
- D) `==` solo funciona con números

**54.** ¿Qué es un callback en JavaScript?
- A) Una función que retorna otra función
- B) Una función que se pasa como argumento a otra función para ejecutarse después
- C) Una variable que almacena el resultado de una función
- D) Un método para cancelar una Promise

**55.** ¿Qué hace `Array.from({length: 3}, (_, i) => i)`?
- A) Crea el array `[1, 2, 3]`
- B) Crea el array `[0, 1, 2]`
- C) Genera un array de tres `undefined`
- D) Lanza un error de sintaxis

**56.** ¿Qué etiqueta HTML es más semántica para la barra de navegación?
- A) `<div id="nav">`
- B) `<navigation>`
- C) `<nav>`
- D) `<menu>`

**57.** ¿Cuál es el valor por defecto de `position` en CSS?
- A) `relative`
- B) `absolute`
- C) `static`
- D) `fixed`

**58.** ¿Qué hace `position: absolute` en CSS?
- A) Fija el elemento en la pantalla independientemente del scroll
- B) Posiciona el elemento relativo a su ancestro posicionado más cercano
- C) Mantiene el elemento en su flujo normal
- D) Centra el elemento en el viewport

**59.** ¿Qué significa que React usa un Virtual DOM?
- A) Que no usa HTML real
- B) Que mantiene una copia en memoria del DOM para calcular cambios mínimos antes de actualizar el DOM real
- C) Que los componentes no se montan en el DOM
- D) Que React usa su propio lenguaje de marcado

**60.** ¿Qué es `package.json`?
- A) Un archivo de configuración de React
- B) El archivo que describe las dependencias y configuración de un proyecto Node/JS
- C) El punto de entrada de la aplicación
- D) Un archivo de configuración de Git

**61.** ¿Cómo se instala una dependencia de desarrollo con npm?
- A) `npm install paquete`
- B) `npm install paquete --save-dev` o `npm install -D paquete`
- C) `npm install paquete --dev-only`
- D) `npm add dev paquete`

**62.** ¿Qué hace `npm run dev`?
- A) Instala dependencias de desarrollo
- B) Ejecuta el script `dev` definido en `package.json`
- C) Borra los archivos de desarrollo
- D) Publica el proyecto en npm

**63.** ¿Qué es `node_modules`?
- A) El código fuente del proyecto
- B) La carpeta donde npm instala las dependencias del proyecto
- C) Los módulos nativos del proyecto
- D) La configuración del servidor

**64.** ¿Qué etiqueta HTML define una fila en una tabla?
- A) `<col>`
- B) `<td>`
- C) `<tr>`
- D) `<th>`

**65.** ¿Qué propiedad CSS crea una transición suave entre valores?
- A) `animation`
- B) `transform`
- C) `transition`
- D) `keyframe`

**66.** ¿Cuál es la forma correcta de importar un componente en React?
- A) `require('./MiComponente')`
- B) `import MiComponente from './MiComponente'`
- C) `include MiComponente from './MiComponente'`
- D) `load('./MiComponente')`

**67.** ¿Qué hace `ReactDOM.createRoot(document.getElementById('root')).render(<App />)`?
- A) Crea un nuevo componente App
- B) Monta la aplicación React en el elemento con id `root` del DOM
- C) Registra el componente globalmente
- D) Renderiza el componente sin adjuntarlo al DOM

**68.** En JavaScript, ¿qué hace el operador ternario `condicion ? a : b`?
- A) Ejecuta `a` y `b` en paralelo
- B) Devuelve `a` si la condición es verdadera, `b` si es falsa
- C) Compara `a` y `b` retornando el mayor
- D) Asigna `a` o `b` según el tipo de la condición

**69.** ¿Qué es `localStorage`?
- A) Una base de datos del servidor
- B) Un espacio de almacenamiento en el navegador que persiste entre sesiones
- C) La memoria RAM del navegador
- D) Un tipo de variable global en Node.js

**70.** ¿Qué hace `JSON.stringify()`?
- A) Parsea un string JSON a objeto JavaScript
- B) Convierte un objeto JavaScript a string JSON
- C) Valida si un string es JSON válido
- D) Formatea el JSON para mostrarlo en pantalla

---

## SECCIÓN 2: PREGUNTAS MEDIAS (71–150)

**71.** ¿Cuándo se ejecuta el callback de `useEffect` con un array de dependencias vacío `[]`?
- A) Cada vez que el componente se re-renderiza
- B) Solo una vez, después del primer renderizado (equivalente a `componentDidMount`)
- C) Nunca se ejecuta
- D) Cada vez que cambia cualquier variable de estado

**72.** ¿Cómo se limpia un efecto en `useEffect`?
- A) Llamando a `useEffect.cleanup()`
- B) Retornando una función desde el callback de `useEffect`
- C) Usando `useCleanup()` hook
- D) Pasando `null` como dependencia

**73.** ¿Cuál es el propósito de `useContext`?
- A) Crear un contexto global
- B) Consumir el valor de un contexto de React sin prop drilling
- C) Guardar datos en el localStorage
- D) Sincronizar el estado entre componentes hermanos

**74.** ¿Qué es el "prop drilling"?
- A) Una técnica para optimizar la transmisión de props
- B) Pasar props a través de múltiples niveles de componentes intermedios que no las usan
- C) Una forma de desestructurar props
- D) Un error de React al pasar props incorrectas

**75.** ¿Qué hace `React.createContext()`?
- A) Crea un componente nuevo
- B) Crea un objeto Context que puede ser provisto y consumido por componentes
- C) Registra un hook personalizado
- D) Crea una referencia al DOM

**76.** ¿Cómo se provee un contexto a los componentes hijos?
- A) `<Context value={datos}>`
- B) `<Context.Provider value={datos}>`
- C) `useProvide(Context, datos)`
- D) `Context.provide(datos)`

**77.** ¿Para qué se usa React Router?
- A) Para hacer peticiones HTTP en React
- B) Para gestionar la navegación y las rutas en una SPA de React
- C) Para enrutar eventos entre componentes
- D) Para configurar rutas de API en el backend

**78.** ¿Qué componente de React Router envuelve la aplicación para habilitar el enrutamiento?
- A) `<Switch>`
- B) `<Router>` o `<BrowserRouter>`
- C) `<Routes>`
- D) `<Navigation>`

**79.** ¿Qué hace el componente `<Link>` de React Router?
- A) Redirige al usuario a una URL externa
- B) Navega entre rutas sin recargar la página completa
- C) Carga un componente de forma lazy
- D) Renderiza un `<a>` que envía un formulario

**80.** ¿Cómo se accede a los parámetros de una URL como `/usuarios/:id`?
- A) `useParams()` de React Router
- B) `window.location.params`
- C) `this.props.params`
- D) `useRoute().params`

**81.** ¿Cuál es la diferencia entre `useNavigate` y `<Link>`?
- A) `useNavigate` es para navegación programática, `<Link>` es declarativa en JSX
- B) `<Link>` solo funciona en botones, `useNavigate` en cualquier elemento
- C) `useNavigate` es más lento que `<Link>`
- D) Son completamente intercambiables

**82.** ¿Qué hace `async/await` con el manejo de errores?
- A) Los errores se ignoran automáticamente
- B) Se usa `try/catch` para capturar rechazos de Promises
- C) Se usa `.catch()` directamente en la función async
- D) Los errores siempre se propagan al componente padre

**83.** ¿Cuál es la diferencia entre `Promise.all()` y `Promise.allSettled()`?
- A) No hay diferencia
- B) `Promise.all()` falla si una Promise rechaza; `Promise.allSettled()` espera a todas independientemente
- C) `Promise.allSettled()` es más rápida
- D) `Promise.all()` acepta más Promises que `Promise.allSettled()`

**84.** En React, ¿cuándo se dispara un re-renderizado?
- A) Solo cuando cambian las props
- B) Cuando cambian el estado (`useState`) o las props del componente
- C) En cada animación del navegador
- D) Cuando el usuario hace scroll

**85.** ¿Qué hace `e.preventDefault()` en un formulario de React?
- A) Previene que el componente se re-renderice
- B) Evita el comportamiento por defecto del navegador (como recargar la página al enviar)
- C) Cancela el evento y lo elimina de la cola
- D) Previene la propagación del evento a componentes padres

**86.** ¿Qué es CSS Modules?
- A) Una librería de componentes CSS
- B) Un sistema donde los nombres de clases CSS se generan únicos por archivo, evitando conflictos globales
- C) Los módulos de PostCSS
- D) Una forma de importar Bootstrap en React

**87.** ¿Cómo se importa un CSS Module en React?
- A) `import './styles.module.css'`
- B) `import styles from './styles.module.css'` y se usa `styles.clase`
- C) `require('./styles.module.css')`
- D) `<style module src="./styles.module.css">`

**88.** ¿Qué es un custom hook en React?
- A) Un hook nativo de React que no está documentado
- B) Una función JavaScript que empieza por `use` y puede usar otros hooks
- C) Un componente que expone su estado
- D) Una alternativa a `useEffect` para efectos globales

**89.** ¿Cuál de estas NO es una regla de los hooks de React?
- A) Solo se pueden llamar en el nivel superior (no dentro de condicionales)
- B) Solo se pueden llamar desde componentes funcionales o custom hooks
- C) Solo se pueden usar una vez por componente
- D) No se pueden llamar después de un return condicional

**90.** ¿Qué hace `useRef`?
- A) Crea una referencia reactiva que causa re-renderizado al cambiar
- B) Crea una referencia mutable que persiste entre renders sin causar re-renderizado
- C) Guarda el estado anterior del componente
- D) Referencia el componente padre

**91.** ¿Cuál es la diferencia entre `null` y `undefined` en JavaScript?
- A) Son exactamente iguales
- B) `undefined` es la ausencia involuntaria de valor; `null` es la ausencia intencionada
- C) `null` es un error, `undefined` es un tipo
- D) `undefined` solo existe en Node.js

**92.** ¿Qué hace `Object.keys(obj)`?
- A) Devuelve los valores del objeto
- B) Devuelve un array con las claves del objeto
- C) Crea una copia del objeto
- D) Elimina las claves del objeto

**93.** ¿Cómo se hace una copia superficial de un objeto en JavaScript?
- A) `Object.clone(obj)`
- B) `{...obj}` o `Object.assign({}, obj)`
- C) `obj.copy()`
- D) `JSON.parse(JSON.stringify(obj))` (esto es copia profunda)

**94.** ¿Qué devuelve `[1, [2, [3]]].flat()`?
- A) `[1, 2, 3]`
- B) `[1, 2, [3]]`
- C) `[1, [2, [3]]]` (sin cambios)
- D) `[[1, 2, 3]]`

**95.** ¿Cuándo usarías `useCallback`?
- A) Para memorizar el resultado de un cálculo costoso
- B) Para memorizar una función y evitar que se cree de nuevo en cada renderizado
- C) Para cancelar efectos asíncronos
- D) Para limpiar el estado al desmontar un componente

**96.** ¿Para qué sirve `useMemo`?
- A) Para memorizar una función callback
- B) Para memorizar el resultado de un cálculo costoso entre renders
- C) Para compartir estado entre componentes
- D) Para registrar efectos secundarios

**97.** En React Router v6, ¿cómo se define una ruta?
- A) `<Route path="/ruta" component={Componente} />`
- B) `<Route path="/ruta" element={<Componente />} />`
- C) `<Route to="/ruta" render={Componente} />`
- D) `<Route url="/ruta"><Componente /></Route>`

**98.** ¿Cómo se maneja el estado global sin librerías externas en React?
- A) Con variables globales de JavaScript
- B) Mediante Context API combinado con `useState` o `useReducer`
- C) Con el hook `useGlobal`
- D) No es posible sin librerías externas

**99.** ¿Qué es el ciclo de vida de un componente React?
- A) El tiempo que tarda en renderizarse
- B) Las fases por las que pasa: montado, actualización y desmontado
- C) Los eventos del ciclo de Node.js aplicados a React
- D) La secuencia de props que recibe un componente

**100.** ¿Qué hook de React Router te permite acceder a la ruta actual?
- A) `useRoute()`
- B) `useLocation()`
- C) `usePath()`
- D) `useCurrentRoute()`

**101.** ¿Cuál es la forma correcta de actualizar un objeto en el estado de React?
- A) `setState.property = newValue`
- B) `setObj(prev => ({ ...prev, property: newValue }))`
- C) `obj.property = newValue; setObj(obj)`
- D) `setObj({ property: newValue })`

**102.** ¿Cuándo usar `useReducer` en lugar de `useState`?
- A) Cuando el estado es un string
- B) Cuando la lógica de actualización del estado es compleja o involucra múltiples sub-valores
- C) Cuando se necesita estado global
- D) `useReducer` siempre es mejor que `useState`

**103.** ¿Qué hace el evento `onChange` en un `<input>` de React?
- A) Se dispara cuando el input pierde el foco
- B) Se dispara cuando el valor del input cambia
- C) Se dispara cuando el formulario se envía
- D) Se dispara cuando el input recibe el foco

**104.** ¿Cómo se condiciona el renderizado de un componente en React?
- A) Con `if/else` directamente en el JSX
- B) Con el operador `&&` o el operador ternario dentro de JSX
- C) Con la prop `condition`
- D) Con `React.conditional()`

**105.** ¿Qué hace `React.Fragment` o `<>`?
- A) Crea un elemento invisible en el DOM
- B) Agrupa múltiples elementos JSX sin añadir un nodo extra al DOM
- C) Encapsula un componente para que sea reutilizable
- D) Marca un componente como fragmentado para lazy loading

**106.** ¿Cómo se importan múltiples exports nombrados de un módulo?
- A) `import { A, B, C } from './modulo'`
- B) `import A, B, C from './modulo'`
- C) `import [A, B, C] from './modulo'`
- D) `require('./modulo').A, B, C`

**107.** ¿Qué es el event bubbling en JavaScript?
- A) Los errores que suben por el árbol de componentes React
- B) La propagación de eventos desde el elemento hijo hasta sus ancestros en el DOM
- C) El proceso de crear bubbles de estado en React
- D) Una técnica de optimización del event loop

**108.** ¿Cómo se evita que un evento se propague a elementos padre?
- A) `e.preventDefault()`
- B) `e.stopPropagation()`
- C) `e.cancel()`
- D) `e.prevent()`

**109.** ¿Qué es la renderización condicional en React?
- A) Renderizar componentes solo en producción
- B) Mostrar u ocultar componentes o JSX basándose en condiciones
- C) Renderizar componentes de forma aleatoria
- D) Optimizar el rendimiento con renderizado diferido

**110.** ¿Qué hace `Array.prototype.reduce()`?
- A) Reduce el tamaño del array eliminando duplicados
- B) Acumula los elementos del array en un único valor usando una función reductora
- C) Filtra los elementos menores a un umbral
- D) Aplana un array multidimensional

**111.** ¿Cómo se maneja un error en una petición `fetch`?
- A) `fetch` lanza un error automáticamente con códigos 4xx/5xx
- B) Hay que verificar `response.ok` y lanzar el error manualmente, o usar `try/catch`
- C) Con el evento `onerror` del objeto fetch
- D) Con el método `fetch.catch()`

**112.** ¿Qué hace `Promise.race()`?
- A) Ejecuta todas las promesas en secuencia
- B) Resuelve o rechaza tan pronto como la primera Promise del array se resuelva o rechace
- C) Cancela todas las demás Promises cuando una termina
- D) Ejecuta las Promises en orden de prioridad

**113.** ¿Qué es Vite?
- A) Un framework fullstack de JavaScript
- B) Una herramienta de build y servidor de desarrollo ultra-rápida para proyectos frontend
- C) Un gestor de paquetes alternativo a npm
- D) Una librería de testing para React

**114.** ¿Para qué sirve el archivo `vite.config.js`?
- A) Para configurar las variables de entorno
- B) Para configurar el comportamiento de Vite (plugins, servidor, build, alias, etc.)
- C) Para declarar las rutas de la aplicación
- D) Para definir los componentes globales

**115.** ¿Cómo se acceden a variables de entorno en Vite?
- A) `process.env.VARIABLE`
- B) `import.meta.env.VITE_VARIABLE`
- C) `env.VARIABLE`
- D) `window.env.VARIABLE`

**116.** ¿Qué hace `useEffect` cuando una dependencia cambia?
- A) Solo se ejecuta la función de limpieza, no el efecto
- B) Ejecuta la función de limpieza del efecto anterior y luego el nuevo efecto
- C) Cancela el efecto anterior sin ejecutar la limpieza
- D) Reinicia el componente completamente

**117.** En JavaScript, ¿qué es el closure?
- A) Un método para cerrar conexiones WebSocket
- B) Una función que tiene acceso al scope de su función externa, incluso después de que esta haya terminado
- C) Un bloque de código que maneja errores
- D) Una técnica para encapsular módulos

**118.** ¿Cómo se renderiza contenido HTML seguro como string en React?
- A) Directamente en JSX
- B) Con la prop `dangerouslySetInnerHTML={{ __html: contenido }}`
- C) Con `React.renderHTML(contenido)`
- D) Con la etiqueta `<html-content>`

**119.** ¿Qué significa SPA (Single Page Application)?
- A) Una aplicación con una sola pantalla de inicio de sesión
- B) Una aplicación web que carga una sola página HTML y actualiza el contenido dinámicamente sin recargar
- C) Una aplicación que solo funciona en navegadores modernos
- D) Una aplicación servida desde un solo servidor

**120.** ¿Qué hace el hook `useId` de React?
- A) Genera un ID único para identificar el componente internamente
- B) Genera un ID único y estable para asociar elementos de formulario con sus labels
- C) Retorna el ID del usuario autenticado
- D) Crea un identificador para el contexto

**121.** ¿Qué es TypeScript?
- A) Un lenguaje completamente diferente a JavaScript
- B) Un superset de JavaScript que añade tipado estático opcional
- C) El lenguaje que usa React internamente
- D) Una versión antigua de JavaScript

**122.** ¿Cómo se define el tipo de las props en un componente React con TypeScript?
- A) `Component.propTypes = {}`
- B) Con una interface o type: `interface Props { nombre: string }` y `({ nombre }: Props)`
- C) Con el decorador `@Props`
- D) Con `Component.types = {}`

**123.** En TypeScript, ¿qué hace el operador `?` en una propiedad: `nombre?: string`?
- A) La hace de solo lectura
- B) La hace opcional (puede ser `string` o `undefined`)
- C) La convierte en un getter
- D) Declara la propiedad como nullable exclusivamente

**124.** ¿Qué es la prop `children` en React?
- A) Los subcomponentes declarados en el componente padre
- B) El contenido JSX que se pasa entre las etiquetas de apertura y cierre de un componente
- C) Los componentes que un componente puede renderizar
- D) Una prop especial para listas de elementos

**125.** ¿Cuándo usarías `React.memo()`?
- A) Para memorizar el resultado de un hook
- B) Para evitar el re-renderizado de un componente si sus props no han cambiado
- C) Para acceder a la memoria del sistema
- D) Para crear memoización de contextos

**126.** ¿Qué hace `Object.entries(obj)`?
- A) Devuelve solo los valores del objeto
- B) Devuelve un array de pares `[clave, valor]`
- C) Convierte el objeto en un Map
- D) Itera sobre el objeto

**127.** ¿Qué es el optional chaining `?.` en JavaScript?
- A) Una forma de declarar propiedades opcionales
- B) Un operador que retorna `undefined` si el valor a la izquierda es `null` o `undefined`, en lugar de lanzar error
- C) Una forma abreviada del operador ternario
- D) Un operador de TypeScript que no existe en JavaScript

**128.** ¿Qué hace el operador nullish coalescing `??`?
- A) Comprueba si un valor es null o undefined
- B) Retorna el operando derecho si el izquierdo es `null` o `undefined`
- C) Es equivalente al operador `||`
- D) Asigna un valor solo si la variable no existe

**129.** ¿Cómo se pasan funciones como callbacks a componentes hijos en React?
- A) Solo a través del contexto global
- B) Como props normales: `<Hijo onClickHandler={miFuncion} />`
- C) Usando `useCallback` obligatoriamente
- D) Con el evento `onCallback`

**130.** ¿Qué es la reconciliación en React?
- A) El proceso de combinar dos estados de React
- B) El algoritmo con el que React compara el Virtual DOM anterior con el nuevo para actualizar solo los cambios necesarios en el DOM real
- C) La sincronización entre el frontend y el backend
- D) El proceso de unificar múltiples contextos

**131.** ¿Qué hace `localStorage.setItem('key', value)`?
- A) Lee un valor del localStorage
- B) Guarda un par clave-valor en el localStorage del navegador
- C) Elimina un elemento del localStorage
- D) Verifica si una clave existe en el localStorage

**132.** ¿Cómo se elimina un elemento de un array en React sin mutar el estado?
- A) `arr.splice(index, 1); setArr(arr)`
- B) `setArr(arr.filter((_, i) => i !== index))`
- C) `delete arr[index]; setArr(arr)`
- D) `arr.remove(index); setArr(arr)`

**133.** En Flexbox, ¿qué hace `align-items: center`?
- A) Centra los elementos en el eje principal
- B) Centra los elementos en el eje transversal
- C) Distribuye los elementos uniformemente
- D) Alinea el texto dentro de los flex items

**134.** ¿Cómo se hace un fetch con método POST en JavaScript?
- A) `fetch(url, { type: 'POST', data: body })`
- B) `fetch(url, { method: 'POST', headers: {...}, body: JSON.stringify(data) })`
- C) `fetch.post(url, data)`
- D) `post(url, { body: data })`

**135.** ¿Qué hace `git stash`?
- A) Elimina los cambios no confirmados
- B) Guarda temporalmente los cambios locales no confirmados para limpiar el working tree
- C) Confirma los cambios con un mensaje automático
- D) Aplica los últimos cambios del repositorio remoto

**136.** ¿Qué es una Higher-Order Function (HOF) en JavaScript?
- A) Una función con muchos parámetros
- B) Una función que recibe funciones como argumentos y/o devuelve una función
- C) Una función recursiva
- D) Una función que accede a variables globales

**137.** ¿Qué hace `Array.isArray(valor)`?
- A) Convierte el valor en array
- B) Devuelve `true` si el valor es un array, `false` si no
- C) Comprueba si todos los elementos del array cumplen una condición
- D) Valida que el array no esté vacío

**138.** ¿Qué es el contexto (`this`) en JavaScript?
- A) El componente React actual
- B) El objeto al que pertenece la función en el momento de ser llamada
- C) El módulo que importó la función
- D) El elemento del DOM que disparó el evento

**139.** ¿Para qué sirve `useLayoutEffect`?
- A) Es idéntico a `useEffect`
- B) Se ejecuta sincrónicamente después de todos los cambios del DOM, antes de que el navegador pinte
- C) Solo se ejecuta en el servidor (SSR)
- D) Aplica estilos inline de forma reactiva

**140.** ¿Qué es el patrón "lifting state up" en React?
- A) Mover el estado a un servidor externo
- B) Elevar el estado al ancestro común más cercano para compartirlo entre componentes hermanos
- C) Usar el contexto global para cualquier estado
- D) Optimizar el estado moviendo cálculos a useMemo

**141.** ¿Cuál es la diferencia entre renderizado del lado del cliente (CSR) y del servidor (SSR)?
- A) No hay diferencia visible para el usuario
- B) En CSR el HTML lo genera el navegador con JS; en SSR el servidor genera el HTML completo y lo envía
- C) CSR usa React y SSR usa otro framework
- D) SSR es más lento siempre

**142.** ¿Qué es Axios y en qué se diferencia de `fetch`?
- A) Axios es más moderno y nativo del navegador
- B) Axios es una librería HTTP con API más conveniente (interceptors, transformación automática, cancelación) mientras que `fetch` es nativo del navegador
- C) Axios solo funciona en Node.js
- D) No hay diferencias relevantes

**143.** ¿Cómo se maneja el estado de carga (loading) en un fetch de React?
- A) Automáticamente con el hook `useFetch`
- B) Con un estado booleano `isLoading` que se actualiza antes y después de la petición
- C) Usando el evento `onLoad` del fetch
- D) Con `Suspense` siempre

**144.** ¿Qué hace `Array.prototype.some()`?
- A) Devuelve el primer elemento que cumple la condición
- B) Devuelve `true` si al menos un elemento cumple la condición
- C) Devuelve `true` solo si todos los elementos cumplen la condición
- D) Suma los elementos que cumplen la condición

**145.** ¿Qué es `json-server`?
- A) Una alternativa a Node.js para servir JSON
- B) Una herramienta que crea una API REST fake a partir de un archivo JSON, útil para prototipar frontends
- C) El servidor HTTP que viene con React
- D) Un middleware de Express para parsear JSON

**146.** ¿Qué hace el método `toString()` en JavaScript?
- A) Convierte un objeto a JSON
- B) Convierte un valor a su representación string
- C) Devuelve la longitud del string
- D) Serializa el objeto para almacenarlo

**147.** ¿Qué es el patrón Compound Components en React?
- A) Componentes que se compilan juntos
- B) Un patrón donde varios componentes relacionados comparten estado implícito y se usan juntos
- C) Componentes que usan más de un hook
- D) Componentes creados con herencia de clases

**148.** ¿Qué hace `git pull`?
- A) Sube los cambios locales al repositorio remoto
- B) Descarga los cambios del repositorio remoto y los integra en la rama actual
- C) Crea una copia local del repositorio remoto
- D) Lista las ramas disponibles en el remoto

**149.** ¿Cómo se usa el hook `useSearchParams` de React Router?
- A) Para buscar componentes por nombre
- B) Para leer y modificar los query params de la URL
- C) Para buscar elementos en el estado
- D) Para filtrar las rutas disponibles

**150.** ¿Qué es el debouncing en el contexto de inputs de búsqueda?
- A) Una técnica CSS para suavizar animaciones
- B) Retrasar la ejecución de una función hasta que el usuario deja de escribir por un tiempo determinado, reduciendo peticiones
- C) Limpiar el input después de enviar el formulario
- D) Validar el input mientras el usuario escribe

---

## SECCIÓN 3: PREGUNTAS DIFÍCILES (151–200)

**151.** ¿Qué problema resuelve el patrón de custom hook `useSafeAsync`?
- A) Ejecuta peticiones HTTP de forma más eficiente
- B) Evita actualizar el estado de un componente ya desmontado, previniendo memory leaks
- C) Cancela las peticiones automáticamente con AbortController siempre
- D) Transforma errores de red en mensajes amigables

**152.** ¿Cuál es la salida de este código?
```js
const [a, ...b] = [1, 2, 3, 4];
console.log(b);
```
- A) `[1, 2, 3, 4]`
- B) `[2, 3, 4]`
- C) `2`
- D) `[1]`

**153.** ¿Qué renderiza este componente?
```jsx
function Comp({ items }) {
  return <>{items.length > 0 && <ul>{items.map(i => <li key={i}>{i}</li>)}</ul>}</>;
}
```
cuando `items = [0]`
- A) Una `<ul>` con un `<li>` que contiene `0`
- B) Nada, porque `0` es falsy
- C) El texto literal `"0"`
- D) Un error de React

**154.** ¿Por qué puede causar un bug renderizar `{items.length && <Comp />}` en lugar de `{items.length > 0 && <Comp />}`?
- A) No hay diferencia
- B) Cuando el array está vacío, `items.length` es `0` (falsy pero se renderiza como texto "0" en React)
- C) El operador `&&` no funciona con números
- D) React lanza un error cuando el lado izquierdo es un número

**155.** ¿Qué problema tienen los hooks llamados dentro de condicionales?
- A) Reducen el rendimiento
- B) Violan la regla de orden de hooks: React depende de que se llamen en el mismo orden en cada render
- C) No tienen acceso al estado del componente
- D) Solo afectan al modo estricto de React

**156.** ¿Cuál de estos patrones es una optimización incorrecta con `useCallback`?
- A) Memorizar un handler de evento para pasarlo a un componente memo'd
- B) Memorizar absolutamente todas las funciones de un componente sin importar si se pasan como props
- C) Usar `useCallback` en una función que es dependencia de `useEffect`
- D) Compartir callbacks entre componentes hermanos mediante contexto

**157.** ¿Qué es el "stale closure" problem en hooks de React?
- A) Cuando un componente no se desmonta correctamente
- B) Cuando una función memorizada con `useCallback` o dentro de `useEffect` captura valores de estado desactualizados porque no se añaden como dependencias
- C) Cuando el estado se actualiza demasiado rápido
- D) Cuando `useEffect` se ejecuta antes de que el DOM esté listo

**158.** En el siguiente código, ¿cuántas veces se renderiza `Hijo`?
```jsx
function Padre() {
  const [count, setCount] = useState(0);
  const handler = () => {};
  return <Hijo onClick={handler} />;
}
const Hijo = React.memo(({ onClick }) => <button onClick={onClick}>Click</button>);
```
- A) Solo una vez siempre
- B) Se re-renderiza en cada actualización del padre, porque `handler` es una nueva función en cada render
- C) Se re-renderiza solo cuando cambia `count`
- D) Nunca se re-renderiza

**159.** ¿Qué patrón resuelve el problema anterior?
- A) Usar `useMemo` en lugar de `React.memo`
- B) Envolver `handler` en `useCallback(()=>{}, [])`
- C) Pasar `onClick` como string
- D) Usar `useRef` para la función

**160.** ¿Cuál es el output de este código JavaScript?
```js
console.log(typeof (() => {}));
```
- A) `"arrow"`
- B) `"function"`
- C) `"object"`
- D) `"undefined"`

**161.** ¿Qué hace el siguiente código con el estado?
```jsx
const [arr, setArr] = useState([1, 2, 3]);
arr.push(4);
setArr(arr);
```
- A) Añade correctamente el 4 y re-renderiza
- B) Puede no disparar un re-renderizado porque la referencia al array no cambia
- C) Lanza un error porque no se puede mutar el estado
- D) Actualiza el estado pero solo en el siguiente ciclo

**162.** ¿Qué es el "batching" de actualizaciones de estado en React 18?
- A) Procesar todas las actualizaciones de estado en lotes para reducir el número de re-renders
- B) Una técnica para enviar múltiples peticiones HTTP a la vez
- C) Agregar elementos a arrays de estado en lote
- D) Sincronizar estado entre pestañas del navegador

**163.** ¿Qué devuelve el siguiente código?
```js
[1, 2, 3].reduce((acc, curr) => acc + curr, 0)
```
- A) `[1, 2, 3]`
- B) `6`
- C) `"123"`
- D) `NaN`

**164.** En React, ¿por qué es problemático usar el índice del array como `key` en listas dinámicas?
- A) Los índices son más lentos que los IDs
- B) Cuando los elementos se reordenan o eliminan, los índices cambian y React puede reutilizar el estado incorrecto
- C) React no acepta números como keys
- D) Los índices generan warnings en TypeScript

**165.** ¿Qué hace `AbortController` en el contexto de `fetch`?
- A) Controla los permisos de acceso a la API
- B) Permite cancelar peticiones `fetch` en curso, útil para cancelar requests al desmontar componentes
- C) Controla el orden de ejecución de múltiples fetches
- D) Aborta el renderizado del componente si la petición falla

**166.** ¿Cuál es la diferencia entre un `Error Boundary` y `try/catch` en React?
- A) Son equivalentes
- B) Los `Error Boundaries` capturan errores en el árbol de componentes (rendering, lifecycle); `try/catch` captura errores síncronos en código imperativo
- C) `try/catch` captura errores de renderizado y `Error Boundary` solo errores de red
- D) Los `Error Boundaries` solo funcionan en producción

**167.** ¿Qué es el patrón "render props"?
- A) Pasar un string HTML como prop para renderizarlo
- B) Una técnica donde un componente recibe una función como prop que retorna JSX, permitiendo compartir lógica
- C) Props que controlan cómo se renderiza el componente padre
- D) Props especiales que solo se pasan en el renderizado inicial

**168.** ¿En qué casos es preferible `useReducer` sobre `useState`?
- A) Cuando el estado es un objeto complejo con múltiples sub-valores relacionados y transiciones bien definidas
- B) Siempre que el estado cambie más de una vez
- C) Solo cuando se usa Redux
- D) Cuando el estado debe ser compartido entre componentes

**169.** ¿Qué es `Suspense` en React y para qué se usa actualmente?
- A) Para mostrar un fallback mientras un componente cargado con `React.lazy` se está importando
- B) Para pausar el renderizado de componentes pesados
- C) Para manejar errores asíncronos en componentes
- D) Para retardar la hidratación en SSR

**170.** ¿Qué hace `React.lazy()`?
- A) Hace que el componente se renderice lentamente para debug
- B) Carga el componente de forma diferida (code splitting), solo cuando se necesita
- C) Crea una versión memorizada del componente
- D) Registra el componente para carga asíncrona en el servidor

**171.** ¿Cuál es la complejidad temporal de buscar un elemento por clave en un objeto JavaScript?
- A) O(n)
- B) O(log n)
- C) O(1) en promedio
- D) O(n²)

**172.** ¿Qué problema puede causar el siguiente patrón de useEffect?
```jsx
useEffect(() => {
  fetchData().then(data => setData(data));
}, []);
```
- A) Se hace demasiadas peticiones
- B) Si el componente se desmonta antes de que termine el fetch, actualizar el estado causará un memory leak/warning
- C) El efecto se ejecuta dos veces en desarrollo
- D) No es posible usar `then` dentro de `useEffect`

**173.** ¿Qué hace `createPortal` de React?
- A) Crea un punto de entrada alternativo para la aplicación
- B) Renderiza un componente hijo en un nodo del DOM que está fuera de la jerarquía del componente padre
- C) Crea un portal de comunicación entre workers
- D) Permite inyectar componentes en el servidor

**174.** ¿Qué es el "hydration" en el contexto de SSR con React?
- A) Añadir estilos a los componentes después de cargar
- B) El proceso por el cual React en el cliente toma el HTML estático generado por el servidor y le añade interactividad
- C) Sincronizar el estado entre servidor y cliente
- D) Cargar los datos de la API en el estado inicial

**175.** ¿Cuál es la diferencia entre `useEffect` y `useLayoutEffect` respecto al timing?
- A) No hay diferencia en el timing
- B) `useEffect` se ejecuta de forma asíncrona después de que el navegador pinta; `useLayoutEffect` se ejecuta síncronamente después de las mutaciones del DOM pero antes de pintar
- C) `useLayoutEffect` siempre es más rápido
- D) `useEffect` bloquea el render; `useLayoutEffect` no

**176.** En TypeScript con React, ¿qué tipo usarías para tipar el ref de un input HTML?
- A) `React.Ref<HTMLElement>`
- B) `React.RefObject<HTMLInputElement>`
- C) `React.MutableRefObject<Input>`
- D) `HTMLInputElement | null`

**177.** ¿Qué hace el siguiente código y cuándo fallaría?
```tsx
const value = data?.user?.profile?.avatar ?? 'default.png';
```
- A) Siempre devuelve `'default.png'`
- B) Navega de forma segura por propiedades anidadas, devolviendo `'default.png'` solo si algún valor es `null` o `undefined`
- C) Falla si `profile` es un string vacío
- D) Lanza error si `data` es `undefined`

**178.** ¿Cuál es el problema de rendimiento en este componente?
```jsx
function Lista({ datos }) {
  const procesados = datos.map(d => procesarComplejo(d));
  return <ul>{procesados.map(item => <li key={item.id}>{item.nombre}</li>)}</ul>;
}
```
- A) El map devuelve el tipo incorrecto
- B) `procesarComplejo` se ejecuta en cada render aunque `datos` no haya cambiado
- C) No hay problema, React es suficientemente eficiente
- D) El `key` debería ser el índice

**179.** ¿Cómo se soluciona el problema del componente anterior?
- A) `const procesados = useCallback(() => datos.map(...), [datos])`
- B) `const procesados = useMemo(() => datos.map(d => procesarComplejo(d)), [datos])`
- C) `const procesados = useRef(datos.map(...))`
- D) Mover el procesamiento fuera del componente

**180.** ¿Qué ocurre si dos actualizaciones de estado ocurren en el mismo event handler en React 18?
- A) Se realizan dos re-renders separados
- B) Se agrupan (batch) automáticamente en un solo re-render
- C) La segunda actualización cancela la primera
- D) Se lanza un error de concurrencia

**181.** ¿Qué es el "concurrent mode" en React 18?
- A) Un modo para ejecutar React en múltiples hilos
- B) Un conjunto de características que permiten a React interrumpir y reanudar el renderizado para mantener la UI responsiva
- C) El modo de React para aplicaciones con múltiples usuarios concurrentes
- D) Una técnica de renderizado específica para SSR

**182.** ¿Para qué sirve el hook `useTransition` de React 18?
- A) Para animar transiciones entre páginas
- B) Para marcar actualizaciones de estado como no urgentes, permitiendo que React las interrumpa si hay actualizaciones más urgentes
- C) Para gestionar transiciones de CSS
- D) Para manejar animaciones con Framer Motion

**183.** ¿Qué hace el siguiente código TypeScript y cuál es el tipo inferido de `result`?
```ts
const result = [1, 'hola', true].filter((x): x is string => typeof x === 'string');
```
- A) Filtra strings pero el tipo inferido es `(string | number | boolean)[]`
- B) Filtra strings y el tipo inferido es `string[]` gracias al type predicate
- C) Lanza un error de TypeScript por el array mixto
- D) El tipo es `boolean[]`

**184.** ¿Qué patrón implementa este código?
```tsx
function withAuth(Component) {
  return function AuthenticatedComponent(props) {
    const { isAuth } = useAuth();
    if (!isAuth) return <Redirect to="/login" />;
    return <Component {...props} />;
  };
}
```
- A) Render Props
- B) Higher-Order Component (HOC)
- C) Compound Components
- D) Context Provider

**185.** ¿Cuál es el problema potencial con este código de contexto?
```jsx
function AppProvider({ children }) {
  const [user, setUser] = useState(null);
  const [theme, setTheme] = useState('dark');
  return (
    <AppContext.Provider value={{ user, setUser, theme, setTheme }}>
      {children}
    </AppContext.Provider>
  );
}
```
- A) El contexto no puede tener múltiples valores
- B) Cualquier cambio en `user` o `theme` re-renderiza todos los consumidores del contexto, aunque solo necesiten uno de los valores
- C) `setUser` y `setTheme` no deberían estar en el contexto
- D) No hay ningún problema

**186.** ¿Cómo se solucionaría el problema del contexto anterior?
- A) Usando Redux en su lugar
- B) Dividiendo en contextos separados: uno para `user` y otro para `theme`
- C) Memorizando los valores con `useMemo`
- D) Usando `useReducer` en lugar de dos `useState`

**187.** ¿Qué es `Zustand` y en qué mejora sobre Context API?
- A) Una librería de routing para React
- B) Una librería de gestión de estado ligera que evita re-renders innecesarios seleccionando solo el estado necesario
- C) Una alternativa a React para aplicaciones complejas
- D) Un framework de testing para React

**188.** ¿Qué hace `useImperativeHandle`?
- A) Permite a un componente padre llamar métodos del componente hijo a través de un ref
- B) Maneja errores imperativos del DOM
- C) Expone el DOM nativo del componente a los padres
- D) Crea efectos síncronos imperativos

**189.** ¿Cuál es la salida del siguiente código?
```js
const obj = { a: 1 };
const obj2 = { ...obj, a: 2, b: 3 };
console.log(obj2);
```
- A) `{ a: 1, b: 3 }`
- B) `{ a: 2, b: 3 }`
- C) `{ a: 1, a: 2, b: 3 }`
- D) `{ a: 1 }`

**190.** En testing con Vitest y React Testing Library, ¿qué principio guía la forma de escribir tests?
- A) Testear la implementación interna del componente
- B) Testear el comportamiento desde la perspectiva del usuario, no los detalles de implementación
- C) Usar snapshots para todos los tests
- D) Testear cada función por separado con mocks totales

**191.** ¿Qué hace `screen.getByRole('button', { name: /enviar/i })` en React Testing Library?
- A) Selecciona el botón por su clase CSS
- B) Busca un elemento con rol `button` y texto accesible que coincida con "enviar" (case-insensitive)
- C) Busca el botón por su ID en el DOM
- D) Selecciona todos los botones del componente

**192.** ¿Cuándo usarías `waitFor` en React Testing Library?
- A) Para esperar a que los estilos CSS se apliquen
- B) Para esperar a que cambios asíncronos (como peticiones fetch) se reflejen en el DOM
- C) Para pausar el test y hacer debug manual
- D) Para esperar a que el componente se monte por primera vez

**193.** ¿Qué hace `vi.mock('./api')` en Vitest?
- A) Importa el módulo real de la API para testing
- B) Reemplaza el módulo `./api` con una versión mock automática para el test
- C) Verifica que la API ha sido llamada
- D) Deshabilita las llamadas HTTP durante el test

**194.** ¿Cuál es el purpose de `beforeEach` y `afterEach` en Vitest?
- A) Ejecutar código antes y después de todos los tests del archivo
- B) Ejecutar código de setup/teardown antes y después de cada test individual
- C) Definir tests que siempre deben pasar
- D) Agrupar tests relacionados

**195.** ¿Qué es `userEvent` de Testing Library y cuándo se prefiere sobre `fireEvent`?
- A) Son equivalentes
- B) `userEvent` simula interacciones de usuario más realistas (incluyendo eventos intermedios), preferido para tests de integración que replican comportamiento real
- C) `fireEvent` es más moderno y debería usarse siempre
- D) `userEvent` solo funciona con formularios

**196.** ¿Qué hace este código de TypeScript y cuál es el tipo de `keys`?
```ts
function getKeys<T extends object>(obj: T): (keyof T)[] {
  return Object.keys(obj) as (keyof T)[];
}
const keys = getKeys({ a: 1, b: 'hola' });
```
- A) Devuelve `string[]`
- B) Devuelve `("a" | "b")[]`
- C) Lanza error de tipos
- D) Devuelve `(string | number)[]`

**197.** ¿Qué es code splitting y cómo lo implementas en React?
- A) Dividir el CSS en múltiples archivos
- B) Dividir el bundle de JavaScript en chunks que se cargan bajo demanda, implementado con `React.lazy()` y `Suspense`
- C) Separar el código del servidor del cliente
- D) Dividir componentes grandes en componentes más pequeños

**198.** ¿Cuál de estos antipatrones en React puede causar renders infinitos?
- A) Llamar a `setState` directamente en el cuerpo del componente sin condición
- B) Usar `useState` para un contador
- C) Pasar funciones como props a componentes hijo
- D) Renderizar listas con `map()`

**199.** ¿Qué herramienta de testing genera informes de cobertura de código en proyectos Vite?
- A) `jest --coverage`
- B) `vitest --coverage` con el proveedor `@vitest/coverage-v8`
- C) `npm test --report`
- D) `vite test --coverage`

**200.** ¿Qué patrón implementa el siguiente custom hook y cuándo lo usarías?
```jsx
function useDebounce(value, delay) {
  const [debouncedValue, setDebouncedValue] = useState(value);
  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debouncedValue;
}
```
- A) Throttling; para limitar el número de eventos por segundo
- B) Debouncing; para retrasar la actualización de un valor hasta que el input deja de cambiar, reduciendo peticiones de búsqueda
- C) Memoización; para evitar re-cálculos costosos
- D) Polling; para actualizar datos a intervalos regulares
